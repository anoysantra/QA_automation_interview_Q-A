-Explanation of the pytest framework and its uses and why widely used and its whole complete working architecture.
-Parametarization of Datas in Test Cases.
-use of conftest.py
-why pytest.ini is written
-Custom CLI Options (pytest_addoption) [ parser.addoption("--env", action="store", default="qa", help="Environment: qa, staging, or prod")]
-Soft Assertions (pytest-check)
-difference between @pytest.mark.parametrize and Fixture Parametrization(@pytest.fixture(params=["chrome", "firefox", "edge"])
-pytest.mark(for regression,smoke etc)
-pytest report making
-"request" fixture used in pytest. why(and its inbuilt : request.param,request.node,request.addfinalizer)
-other imp inbuilt functions of pytest widely used the common ones till the intermediate level.(pytest.skip,pytest.fail)
-Setup , Call and Teardown
-read the basics of unittest(although I did no hand-on this read about it in general) and how pytest is diff from it.



